<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.diviner.modules.user.dao.UserDao">
    <select id="getUserAll" resultType="sysUser">
      select * from sys_user
    </select>

    <select id="getUserAll1" resultType="map">
      select * from sys_user
    </select>
</mapper>